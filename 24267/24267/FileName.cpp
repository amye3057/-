#include <stdio.h>

void MenOfPassion(int A[], int n);

int main() {
    int n;
    scanf_s("%d", &n);

}

/*
void MenOfPassion(A[], n) {
    sum < -0;
    for i < -1 to n - 2
        for j < -i + 1 to n - 1
            for k < -j + 1 to n
                sum < -sum + A[i] × A[j] × A[k]; # 코드1
                return sum;
}
// 이 알고리즘의 수행 횟수랑 뭐 최고차항을 적으라는데 이 알고리즘을 이해를 못하겠네..?
*/
